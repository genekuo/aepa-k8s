apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: example-ingress
spec:
  rules:
    - host: minikube.local
      http:
        paths:
          - path: /
            backend:
              serviceName: hello-world
              servicePort: 80